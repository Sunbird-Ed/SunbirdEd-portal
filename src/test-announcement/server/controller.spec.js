describe('Announcement controller', function() {
	describe('create announcement method', function() {
		it('should validate the request parameters', function() {

		})

		it('should create announcement if user has create access', function() {

		})

		it('should not create announcement if user has no create access', function() {

		})

	})
})