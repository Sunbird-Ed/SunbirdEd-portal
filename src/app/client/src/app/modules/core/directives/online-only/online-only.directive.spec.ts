import { OnlineOnlyDirective } from './online-only.directive';

describe('OnlineOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlineOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
