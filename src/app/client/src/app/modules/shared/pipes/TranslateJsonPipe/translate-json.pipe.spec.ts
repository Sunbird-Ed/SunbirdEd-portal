import { TranslateJsonPipe } from '../TranslateJsonPipe/translate-json.pipe';

describe('TranslateJsonPipe', () => {
  it('create an instance', () => {
    // const pipe = new TranslateJsonPipe();
    // expect(pipe).toBeTruthy();
  });
});
