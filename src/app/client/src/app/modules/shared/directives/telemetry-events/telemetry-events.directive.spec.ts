import { TelemetryEventsDirective } from './telemetry-events.directive';

describe('TelemetryEventsDirective', () => {
  it('should create an instance', () => {
    const directive = new TelemetryEventsDirective();
    expect(directive).toBeTruthy();
  });
});
