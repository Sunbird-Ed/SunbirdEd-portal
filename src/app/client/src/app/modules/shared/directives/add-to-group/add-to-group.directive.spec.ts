import { AddToGroupDirective } from './add-to-group.directive';

describe('AddToGroupDirective', () => {
  const addToGroupDirective = AddToGroupDirective;
  it('should create an instance', () => {
    expect(addToGroupDirective).toBeTruthy();
  });
});
