export interface PlayerConfig {
    config: {
        sideMenu: {
            showDownload: boolean
            showExit: boolean
        }
    };
    context: any;
    data: any;
    metadata: any;
}
