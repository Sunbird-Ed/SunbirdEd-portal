export interface PlayerConfig {
    config: {
        sideMenu: {
            showDownload: boolean
            showExit: boolean
            showShare: boolean
        }
    };
    context: any;
    data: any;
    metadata: any;
}
