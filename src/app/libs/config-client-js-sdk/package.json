{
  "name": "config-client-js-sdk",
  "version": "1.0.0",
  "description": "Client js sdk library for config services.",
  "main": "index.js",
  "scripts": {
    "test": "mocha --recursive test",
    "test-with-coverage": "nyc --reporter=html --reporter=text mocha --recursive test"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^5.2.0",
    "nyc": "^13.1.0",
    "rewire": "^4.0.1"
  },
  "dependencies": {
    "joi": "^14.0.2",
    "lodash": "^4.17.11",
    "node-cron": "^2.0.3",
    "request": "^2.88.0"
  }
}
