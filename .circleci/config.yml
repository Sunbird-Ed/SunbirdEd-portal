version: 2.1
jobs:
  build:
    machine:
      image: circleci/classic:latest
    steps:
      #- checkout
      - run:
          name: install jq
          command: sudo apt update && sudo apt-get install -y jq
      - run:
          name: run build file
          command: sudo ./build.sh
      


