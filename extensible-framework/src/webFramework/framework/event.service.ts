import { Injectable } from "@angular/core";
//import { EventBus } from '../../lib/eventbus.min'
@Injectable()
export class EventService {
  constructor() { }
  // addEventListener(type: string, callBack: any, scope: any) {
  //   this.EventBus.addEventListener(type, callBack, scope);
  // }
  // dispatchEvent(type: string, data: any, target: any) {
  //   this.EventBus.dispatch(type, data, target);
  // }
  // removeEventListener(type: string, callBack: any, scope: any) {
  //   this.EventBus.removeEventListener(type, callBack, scope);
  // }
}
